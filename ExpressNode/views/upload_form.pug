html 
    head 
        title   Upload
    body 
        form(action="/upload" method="post" enctype="multipart/form-data") 
            p   
                label(for="fname")   파일이름
                input(type="file" name="userfile" multiple) 
            p 
                input(type="submit" value="업로드")   